<div class="h-screen
        md:h-full
        flex
        flex-col
        justify-start
        gap-6
        md:gap-8
        md:bg-white
        md:shadow-lg
        rounded-lg
        px-4
        py-8
        md:p-8
        w-full
        md:w-3/5
        md:transform
        md:transition-all
        hover:shadow-2xl"
>
  <h1 class="text-4xl font-bold text-gray-800 mb-6 border-b-2">
    Message says:
  </h1>

  <div>
    <p class="text-gray-800 text-lg">
      <%= @message.content %>
    </p>

    <%= link_to  "Create a new message", root_url, class: "mt-4 inline-block px-4 py-2 bg-blue-500 text-white rounded hover:bg-blue-600" %>
  </div>
</div>

