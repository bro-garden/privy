<!DOCTYPE html>
<html>
  <head>
    <title>TempMessage</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= javascript_importmap_tags %>

    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>

  <body>
    <% flash.each do |type, msg| %>
      <div class="absolute w-full <%= type == 'notice' ? 'bg-green-100' : 'bg-red-100' %> py-2 text-center ">
        <span class="text-md <%= type == 'notice' ? 'text-green-700' : 'text-red-700' %>"><%= msg %></span>
      </div>
    <% end %>
    <main class="min-h-screen flex items-start justify-start md:items-center md:justify-center p-0 md:p-4">
      <%= yield %>
    </main>
  </body>
</html>
